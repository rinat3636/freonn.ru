# Database (Railway PostgreSQL / local)
DATABASE_URL=postgresql://postgres:PASSWORD@HOST:5432/railway

# Server
PORT=3000
JWT_SECRET=change_me_min_32_chars
APP_PUBLIC_URL=http://localhost:3000

# Freonn Platform — приём лидов из форм сайта
# Укажите публичный URL вебхука платформы, оставьте пустым, если не используется
PLATFORM_LEAD_WEBHOOK_URL=

# Freonn Group API — единый аккаунт (тот же URL, что Railway API)
VITE_FREONN_API_BASE_URL=https://freonn-group-app-production.up.railway.app
